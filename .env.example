# FairShare Environment Variables
# Copy this file to .env.local for local development
# For production, set these in your deployment platform (Vercel, etc.)

# =============================================================================
# REQUIRED - Supabase Configuration
# =============================================================================
# Get these from your Supabase project dashboard: https://app.supabase.com
# Go to Settings > API to find your project URL and keys

# Your Supabase project URL (starts with https://...)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Your Supabase anonymous key (public key, safe to expose in frontend)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Your Supabase service role key (private key, keep secret!)
# Used for server-side operations and database migrations
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# =============================================================================
# OPTIONAL - Authentication Configuration
# =============================================================================
# NextAuth.js configuration (if using custom auth instead of Supabase Auth)

# Random secret for NextAuth.js (generate with: openssl rand -base64 32)
# NEXTAUTH_SECRET=your_nextauth_secret_here

# Your application URL (for OAuth redirects)
# NEXTAUTH_URL=http://localhost:3000

# =============================================================================
# OPTIONAL - OAuth Providers
# =============================================================================
# Google OAuth (if enabling Google sign-in)
# Get these from Google Cloud Console: https://console.cloud.google.com

# Google OAuth Client ID
# GOOGLE_CLIENT_ID=your_google_client_id_here

# Google OAuth Client Secret
# GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# =============================================================================
# DEVELOPMENT ONLY
# =============================================================================
# These are only needed for local development

# Database URL (for Prisma CLI operations)
# Format: postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres
# DATABASE_URL=postgresql://postgres:your_db_password@db.your-project-ref.supabase.co:5432/postgres

# =============================================================================
# PRODUCTION DEPLOYMENT
# =============================================================================
# These are set in your deployment platform (Vercel, Railway, etc.)

# Your production app URL
# NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# =============================================================================
# CI/CD SECRETS (GitHub Actions)
# =============================================================================
# These should be set as GitHub repository secrets, not in .env files
# Go to your GitHub repo > Settings > Secrets and variables > Actions

# Required for CI/CD:
# - SUPABASE_URL (same as NEXT_PUBLIC_SUPABASE_URL above)
# - SUPABASE_ANON_KEY (same as NEXT_PUBLIC_SUPABASE_ANON_KEY above)
# - SUPABASE_SERVICE_ROLE_KEY (same as above)

# Required for Vercel deployment:
# - VERCEL_TOKEN (get from Vercel dashboard > Settings > Tokens)
# - VERCEL_ORG_ID (get from Vercel dashboard > Settings > General)
# - VERCEL_PROJECT_ID (get from your project settings in Vercel)

# Required for database migrations in CI/CD:
# - DATABASE_URL (PostgreSQL connection string for your Supabase database)

# Optional for enhanced CI/CD features:
# - SNYK_TOKEN (for security scanning, get from snyk.io)
# - LHCI_GITHUB_APP_TOKEN (for Lighthouse CI, get from GitHub Apps)
# - SLACK_WEBHOOK_URL (for deployment notifications)
# - CODECOV_TOKEN (for code coverage reports)

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================

# 1. SUPABASE SETUP:
#    - Create account at https://supabase.com
#    - Create new project
#    - Go to Settings > API and copy your URL and keys
#    - Enable authentication providers you want to use
#    - Set up storage buckets for file uploads

# 2. LOCAL DEVELOPMENT:
#    - Copy this file to .env.local
#    - Fill in your Supabase credentials
#    - Run: npm run dev

# 3. PRODUCTION DEPLOYMENT:
#    - Set environment variables in your deployment platform
#    - For Vercel: Project Settings > Environment Variables
#    - For Railway: Project Settings > Variables
#    - For other platforms: consult their documentation

# 4. CI/CD SETUP:
#    - Go to GitHub repo > Settings > Secrets and variables > Actions
#    - Add all required secrets listed above
#    - Push to main branch to trigger deployment

# =============================================================================
# SECURITY NOTES
# =============================================================================

# ⚠️  NEVER commit .env.local or .env files to version control
# ⚠️  Keep your service role key secret - it has admin access to your database
# ⚠️  Use different Supabase projects for development and production
# ⚠️  Regularly rotate your API keys and secrets
# ⚠️  Enable Row Level Security (RLS) in your Supabase database

# =============================================================================
# TROUBLESHOOTING
# =============================================================================

# Common issues:
# - "Invalid API key": Check your Supabase URL and keys are correct
# - "CORS errors": Ensure your domain is added to Supabase allowed origins
# - "Build failures": Verify all required environment variables are set
# - "Database errors": Check your database URL and service role key

# For more help, see:
# - README.md
# - DEPLOYMENT.md
# - Supabase documentation: https://supabase.com/docs
# - Next.js documentation: https://nextjs.org/docs
